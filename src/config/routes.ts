export const ROUTES = {
  dashboard: "/dashboard",
  employees: "/employees",
  branches: "/branches",
  leads: "/leads",
  logs: "/logs",
  attendance: "/attendance",
  lateApprovals: "/attendance/late-approvals",
  lateHistory: "/attendance/late-history",
  settings: "/settings",
} as const;
