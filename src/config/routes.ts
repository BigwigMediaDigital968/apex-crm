export const ROUTES = {
  dashboard: "/dashboard",
  employees: "/employees",
  branches: "/branches",
  leads: "/leads",
  logs: "/logs",
  attendance: "/attendance",
  settings: "/settings",
} as const;